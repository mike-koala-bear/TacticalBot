#!/usr/bin/env python3
"""
Enhanced Homemade Chess Engine - UCI Executable Wrapper
This script makes the enhanced homemade engine available as a UCI-compatible executable.
"""

import sys
import os

# Add the engines directory to the Python path
sys.path.insert(0, os.path.dirname(os.path.abspath(__file__)))

# Import and run the enhanced engine
from homemade_engine import main

if __name__ == "__main__":
    main()